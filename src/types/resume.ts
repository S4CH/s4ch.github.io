export interface Experience {
  img: string;
  jobTitle: string;
  company: string;
  date: string;
  workLength: string;
  location: string;
  tasks: Array<string>;
  techstack: Array<string>;
}

export interface Education {
  img: string;
  school: string;
  study: string;
  date: string;
}
